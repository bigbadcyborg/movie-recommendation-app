# Update DB workflow — user prompts

This file captures **user → agent** prompts for catalog, database, posters, and related docs.

Earlier session (browse UI, comments, backup/migrate tooling, git): **[russell-prompts/update-db-workflow.md](russell-prompts/update-db-workflow.md)**.

---

## Movie catalog, playbook, posters, browse UI (2026-04-27–28)

1. use `@terminals/6.txt` and `@UPDATE_MOVIES_AGENT.md` to add 10 movies

2. did you run npm run seed? did it migrate the old db over?

3. this is the workflow that needs to be done. add this workflow to `UPDATE_MOVIES_AGENT.md` *(plan mode)*

4. Add end-to-end DB workflow to `UPDATE_MOVIES_AGENT.md` — Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself. To-do's from the plan have already been created…

5. im not seeing any movies now

6. im not seeing the new casablanca movie. the `UPDATE_MOVIES_AGENT.md` should instruct the agent to pre-seed, seed, and migrate

7. no poster is found for casablanca. the `UPDATE_MOVIES_AGENT` needs to have the `fetch-posters.js` ran to prevent this.

8. fix casablanca movie poster

9. append my prompts to AI to the `prompts/update-db-workflow.md`

---

*The same numbered block (items 28–36) is appended in [russell-prompts/update-db-workflow.md](russell-prompts/update-db-workflow.md) for one continuous log.*
