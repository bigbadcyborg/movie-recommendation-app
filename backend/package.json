{
  "name": "movie-recommendation-backend",
  "version": "1.0.0",
  "description": "Movie Recommendation System Backend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/db/seed.js",
    "test": "node scripts/save-test-report.js",
    "db:backup": "node scripts/backup-db.js",
    "db:migrate": "node scripts/migrate-from-db.js",
    "seed:migrate": "node scripts/seed-and-migrate.js",
    "preseed": "node scripts/backup-db.js && node src/db/fetch-posters.js"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.1",
    "express": "^4.21.0",
    "express-rate-limit": "^7.5.0",
    "jsonwebtoken": "^9.0.2",
    "sql.js": "^1.11.0"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "rewire": "^9.0.1",
    "supertest": "^7.2.2"
  }
}
